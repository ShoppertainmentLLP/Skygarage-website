services:
  db:
    image: postgres:17-alpine
    container_name: cars911-db
    environment:
      POSTGRES_USER: cars911
      POSTGRES_PASSWORD: cars911dev
      POSTGRES_DB: cars911
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cars911"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
